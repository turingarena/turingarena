interface exampleinterface {
    var int N, M;
    var int64[] A;
    var int64 S;

    function int solve(int u);

    main {
        input N, M;
        alloc A : 1..N;
        for(i : 1..N) {
            input A[i];
        }
        var int u;
        input u;
        call solve(u) -> S;
        output S;
    }
}
