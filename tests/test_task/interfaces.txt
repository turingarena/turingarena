interface exampleinterface {
    var int N, M;
    var int64[] A;
    var int64 S;

    function solve(int u) -> int;

    callback test(int a, int b) -> int {
        output a, b;
        var int c;
        input c;
        return c;
    }

    main {
        input N, M;
        alloc A : 1..N;
        for(i : 1..N) {
            input A[i];
        }
        var int u;
        input u;
        call solve(u) -> S;
        output S;
    }
}
