#include "support_proto.h"
#include <bits/stdc++.h>

int main() {
  driver_init();

  fprintf(stderr, "DRIVER!\n");

  int x;
  scanf("%d", &x);
  
  fprintf(stderr, "DRIVER: Read from stdin: %d\n", x);
}
