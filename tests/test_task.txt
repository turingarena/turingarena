interface exampleinterface {
    var int N, M;
    var int A[];
    var int S;

    function int solve(int u);

    main {
        input N, M;
        for(i : 1..N) {
            input A[i];
        }
        var int u;
        input u;
        call S = solve(u);
        output S;
    }
}
