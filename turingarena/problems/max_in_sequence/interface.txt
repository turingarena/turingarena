function max_index(int n, int[] a) -> int;

main {
    var int n;
    var int[] a;

    input n;
    alloc a : n;
    for(i : n) {
        input a[i];
    }

    var int index;
    call max_index(n, a) -> index;
    output index;
}
