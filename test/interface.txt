            function f(n, a[]);
            function g(i);

            main {
                read n;
                for i to n {
                    read a[i];
                }

                call r = f(n, a);
                write r;

                for i to n {
                    call s = g(i);
                    write s;
                }
            }
