function play(n) callbacks {
    function guess(n);
}

main {
    read n;

    call ans = play(n) callbacks {
        int guess(int n) {
            write n;
            read ans;
            return ans;
        }
    }

    write ans;
}
