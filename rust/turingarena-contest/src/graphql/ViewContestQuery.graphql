mutation AddUserMutation($input: UserInput!) {
    addUser(input: $input) {
        ok
    }
}

mutation DeleteUserMutation($id: String!) {
    deleteUser(id: $id) {
        ok
    }
}

query ViewContestQuery {
    contestView {
        startTime
        endTime
        problems {
            name
        }
    }
}
